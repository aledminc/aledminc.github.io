// Filled in file 4. Reuse useAnimeScope + onScroll() so the entrance plays when
// the band scrolls into view rather than on mount while off-screen.
export default function CareerTimeline() {
  return (
    <section className="container page">
      <h2>Career Timeline</h2>
    </section>
  )
}
